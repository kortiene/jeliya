<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Networking for the in-process iroh/QUIC transport (FFI milestone).
         Harmless under the interim fixture transport; required the moment the
         FfiClient lands. QUIC is UDP carried by INTERNET (no separate perm). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- DEFERRED to the FFI transport milestone (declaring them now would be
         dead config with no service/notification implementation):
         FOREGROUND_SERVICE + FOREGROUND_SERVICE_DATA_SYNC + POST_NOTIFICATIONS
         and a <service android:foregroundServiceType="dataSync"> to keep the
         in-process node alive in short background windows (Android 15 caps
         dataSync FGS at 6h/24h; the push wake-relay covers the rest). -->
    <!-- Predictive back (API 33+): opt in to the OnBackInvokedCallback API —
         the legacy onBackPressed path stops being delivered as targetSdk
         rises. The engine then registers/unregisters the system callback
         from SystemNavigator.setFrameworkHandlesBack; the mobile shell's
         PopScope claims every back and absorbs the nested Rooms navigator's
         canHandlePop:false notifications so the OS is never told to take a
         back the shell policy needs (app/test/predictive_back_test.dart). -->
    <application
        android:label="Jeliya"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
