/// EN catalog parity — GENERATED by scripts/gen-l10n-parity.mjs from
/// app_en.arb (re-run it after every ARB edit). Pins every plain entry
/// verbatim and every parameterized entry as EXACT output under distinct
/// fixture args (a swapped/dropped placeholder changes the pin); ICU
/// plurals are pinned at n=1 AND n=2 so both branches are exercised.
library;

import 'package:flutter_test/flutter_test.dart';

import 'helpers.dart';

void main() {
  test('every plain en entry matches the catalog exactly', () {
    expect(en.bootSyncing, "Syncing…");
    expect(en.bootNotConnected, "Not connected.");
    expect(en.bootContactingDaemon, "Contacting daemon…");
    expect(en.bootRetryingHint, "Retrying with backoff — start jeliyad or pass ?daemon=<port>.");
    expect(en.bootCouldNotStart, "Could not start");
    expect(en.bootStartingDaemon, "starting the daemon…");
    expect(en.bootBinaryNotFound, "jeliyad binary not found — set JELIYAD_BIN or run `cargo build` in the repo.");
    expect(en.bootEvictingIncumbent, "replacing an incompatible daemon with the bundled one…");
    expect(en.bootDaemonStartFailed, "The bundled daemon could not be started on this machine.");
    expect(en.bootDaemonConnectTimeout, "The daemon started but did not accept a connection in time.");
    expect(en.bootFailedGeneric, "An unexpected failure interrupted start-up.");
    expect(en.composerSendMessage, "Send message");
    expect(en.composerShareAFile, "Share a file");
    expect(en.composerHint, "Enter to send · Shift+Enter for a new line · ⎘ to share a file");
    expect(en.composerSharingFile, "Sharing file…");
    expect(en.errPeerUnreachableTitle, "Couldn't reach the inviter");
    expect(en.errPeerUnreachableMessage, "The invite is readable, but this device could not reach the room admin in time.");
    expect(en.errPeerUnreachableAction, "Ask the inviter to keep the room open, then retry. A fresh combined invite can help if the address changed.");
    expect(en.errBadTicketTitle, "This invite can't be used");
    expect(en.errBadTicketMessage, "The ticket is invalid for this identity, malformed, or no longer matches the room invite.");
    expect(en.errBadTicketAction, "Ask for a new invite generated for your current identity ID.");
    expect(en.errTicketExpiredTitle, "This invite expired");
    expect(en.errTicketExpiredMessage, "The room rejected the ticket because its expiry time has passed.");
    expect(en.errTicketExpiredAction, "Ask the inviter to generate a fresh ticket.");
    expect(en.errRoomNotOpenTitle, "Open the room first");
    expect(en.errRoomNotOpenMessage, "This action needs a live room session on your daemon.");
    expect(en.errRoomNotOpenAction, "Open the room, wait for it to sync, then try again.");
    expect(en.errNotAMemberTitle, "You're not an active member");
    expect(en.errNotAMemberMessage, "The signed room history does not currently admit this identity as an active member.");
    expect(en.errNotAMemberAction, "Use a valid invite for this identity or ask the room owner to re-add you.");
    expect(en.errRoomUnknownTitle, "This room isn't local yet");
    expect(en.errRoomUnknownMessage, "The daemon does not have enough room history to open this room.");
    expect(en.errRoomUnknownAction, "Join with an invite, or open the room with a reachable peer hint.");
    expect(en.errFileUnauthorizedTitle, "Not authorized for this file");
    expect(en.errFileUnauthorizedMessage, "Every reachable provider refused the transfer because the signed history does not admit this identity for it.");
    expect(en.errFileUnauthorizedAction, "Ask the sender to re-share the file or re-invite you, then retry.");
    expect(en.errHashMismatchTitle, "Security check failed");
    expect(en.errHashMismatchMessage, "The fetched bytes did not match the file hash. This is a hard stop — the copy is discarded, never shown.");
    expect(en.errHashMismatchAction, "Ask the sender to re-share the file. Do not retry the same copy.");
    expect(en.errConnectionLostTitle, "Daemon connection lost");
    expect(en.errConnectionLostMessage, "The local UI is not connected to jeliyad right now.");
    expect(en.errConnectionLostAction, "Wait for reconnect, then retry the action.");
    expect(en.errInvalidParamsTitle, "This request wasn't valid");
    expect(en.errInvalidParamsMessage, "The daemon rejected one of the values in this request.");
    expect(en.errInvalidParamsAction, "Check what you entered, then try again.");
    expect(en.errIdentityMissingTitle, "No identity on this daemon yet");
    expect(en.errIdentityMissingMessage, "This action needs your identity, and one has not been created here.");
    expect(en.errIdentityMissingAction, "Create your identity first, then retry.");
    expect(en.errIdentityExistsTitle, "An identity already exists");
    expect(en.errIdentityExistsMessage, "This daemon already holds an identity — a second one cannot be created.");
    expect(en.errIdentityExistsAction, "Use the existing identity shown in Settings.");
    expect(en.errFileUnavailableTitle, "File not available right now");
    expect(en.errFileUnavailableMessage, "No provider is online for this file yet.");
    expect(en.errFileUnavailableAction, "Recheck when the sender is back online.");
    expect(en.errFileTooLargeTitle, "This file is too large to share");
    expect(en.errFileTooLargeMessage, "Shares are capped at 100 MiB per file.");
    expect(en.errFileTooLargeAction, "Pick a smaller file, or split the content.");
    expect(en.errFileUnreadableTitle, "This file couldn't be read");
    expect(en.errFileUnreadableMessage, "The picked file could not be opened from disk.");
    expect(en.errFileUnreadableAction, "Check the file still exists and is readable, then retry.");
    expect(en.errPipeDeniedTitle, "Pipe access denied");
    expect(en.errPipeDeniedMessage, "This pipe does not authorize your identity.");
    expect(en.errPipeDeniedAction, "Ask the pipe owner to expose it to your identity.");
    expect(en.errInternalTitle, "The daemon hit an unexpected failure");
    expect(en.errInternalMessage, "This request failed for a reason the daemon could not classify.");
    expect(en.errInternalAction, "Retry; if it keeps failing, copy diagnostics from Settings and report it.");
    expect(en.errUnknownTitle, "Something went wrong");
    expect(en.errUnknownMessage, "The daemon reported an error this app has no specific copy for.");
    expect(en.errUnknownAction, "Open Technical details for the exact error, then retry.");
    expect(en.fetchErrFileUnavailable, "No provider is online for this file yet. Recheck when the sender is back online.");
    expect(en.fetchErrFileUnauthorized, "Every provider refused this fetch — your identity is not authorized for it. Ask the sender to re-share or re-invite you.");
    expect(en.fetchErrHashMismatch, "This file failed a security check and wasn't saved — it may have been corrupted or tampered with in transit.");
    expect(en.fleetSearchPlaceholder, "Search agents…");
    expect(en.fleetSearchAgents, "Search agents");
    expect(en.fleetAddAgent, "＋ Add agent");
    expect(en.fleetFilterAgents, "Filter agents");
    expect(en.fleetFilterAll, "All");
    expect(en.fleetFilterNeedsAttention, "Needs attention");
    expect(en.fleetFilterWorking, "Working");
    expect(en.fleetFilterOffline, "Offline");
    expect(en.fleetStatActiveAgents, "Active agents");
    expect(en.fleetStatWorkingNow, "Agents working now");
    expect(en.fleetStatWorkingNowSub, "live peer + fresh status");
    expect(en.fleetStatRoomCoverage, "Room coverage");
    expect(en.fleetLoadingAgents, "Loading agents");
    expect(en.fleetEmptyNoAgents, "No agents in any room yet. Use “Add agent” to mint an invite.");
    expect(en.fleetEmptyNoMatch, "No agents match this filter.");
    expect(en.fleetLivenessWorking, "Working");
    expect(en.fleetLivenessOnline, "Online");
    expect(en.fleetLivenessStale, "Stale");
    expect(en.fleetLivenessOffline, "Offline");
    expect(en.fleetNoStatusPosted, "No status posted yet.");
    expect(en.fleetNeverSeen, "Never seen");
    expect(en.fleetNeedsAttention, "Needs attention");
    expect(en.fleetNeedsAttentionEmpty, "Nothing needs attention right now.");
    expect(en.fleetAttentionFailed, "Failed");
    expect(en.fleetAttentionReview, "Awaiting review");
    expect(en.fleetAttentionStale, "Stale");
    expect(en.fleetAttentionOffline, "Offline after work");
    expect(en.fleetLastStatusHint, "Last posted status — its liveness no longer supports it");
    expect(en.fleetOpenRoom, "⇱ Open room");
    expect(en.fleetSparkLoading, "Loading status history");
    expect(en.fleetSparkEmpty, "No status history yet");
    expect(en.fleetRelJustNow, "just now");
    expect(en.addAgentTitle, "Add an agent");
    expect(en.addAgentNoOwnedRooms, "You don’t own any rooms yet. Create a room first — agent invites can only be minted for a room you own.");
    expect(en.addAgentIntroEmphasis, "does not start anything");
    expect(en.addAgentRoomLabel, "Room");
    expect(en.addAgentIdentityLabel, "Agent identity ID");
    expect(en.addAgentIdentityPlaceholder, "64-hex identity ID (from jeliya-agent.mjs --identity-only)");
    expect(en.addAgentWorkerLabel, "Worker");
    expect(en.addAgentWorkerEchoOption, "echo (safe — no real execution, for trying the flow)");
    expect(en.addAgentWorkerClaudeOption, "claude (runs real commands — arbitrary code/file execution for this room’s allowlisted senders)");
    expect(en.addAgentClaudeWarning, "WARNING — --worker claude runs the claude CLI with --permission-mode acceptEdits on every triggered message from an allowlisted sender. That is arbitrary code / file execution on this host. Only enable it for a room and senders you trust.");
    expect(en.addAgentMintInvite, "Mint agent invite");
    expect(en.addAgentMinting, "Minting…");
    expect(en.addAgentResultIntro, "Run this on the agent’s machine to bring it into the room. The daemon has no “spawn agent” call — this is copied and run by a human on purpose.");
    expect(en.addAgentLaunchCommandLabel, "Agent launch command");
    expect(en.addAgentCopyCommand, "Copy command");
    expect(en.addAgentTicketOnly, "Ticket only (if you assemble the command yourself):");
    expect(en.addAgentCopyTicket, "Copy ticket");
    expect(en.addAgentNoDialableAddr, "This daemon reported no dialable address — the agent may connect via relay or discovery.");
    expect(en.addAgentNewInvite, "← New invite");
    expect(en.modalCreateRoomTitle, "Create a room");
    expect(en.modalRoomNameLabel, "Room name");
    expect(en.modalRoomNamePlaceholder, "Build Iroh Rooms MVP");
    expect(en.modalCreateRoom, "Create room");
    expect(en.modalCreatingRoom, "Creating…");
    expect(en.modalCreateRoomHomonymWarning, "A room with that name already exists on this device — this one will get its own ID.");
    expect(en.modalJoinRoomTitle, "Join with a ticket");
    expect(en.modalTicketLabel, "Ticket");
    expect(en.modalTicketPlaceholder, "roomtkt1… or roomtkt1…#<endpoint_id>@host:port");
    expect(en.modalPeerAddrLabel, "Peer address");
    expect(en.modalPeerAddrOptional, "(optional)");
    expect(en.modalJoinRoom, "Join room");
    expect(en.modalJoiningRoom, "Joining…");
    expect(en.modalLeaveRoomTitle, "Leave room");
    expect(en.modalLeaveRoom, "Leave room");
    expect(en.modalLeavingRoom, "Leaving…");
    expect(en.modalCancel, "Cancel");
    expect(en.onboardingTagline, "Your rooms, your data. Private by default — built for humans & agents.");
    expect(en.onboardingIdentityTitle, "Create your identity");
    expect(en.onboardingIdentityCopy1, "A keypair generated and stored by your local daemon. No account, no server — the private key never leaves this machine.");
    expect(en.onboardingIdentityCopy2, "There's no password reset and no recovery — if you lose this device or its data folder, this identity is gone for good.");
    expect(en.onboardingCreateIdentity, "Create identity");
    expect(en.onboardingCreatingIdentity, "Creating…");
    expect(en.onboardingYourIdentityId, "Your identity ID");
    expect(en.onboardingIdentityCardCopy1, "Being invited to a room? Send this ID to the inviter first — tickets are bound to it.");
    expect(en.onboardingIdentityCardCopy2, "Peers show up by this same hex ID at first — click any name in a room to set a local nickname for them (only visible to you).");
    expect(en.onboardingCreateRoomCopy, "Start a space and invite people or agents with tickets.");
    expect(en.onboardingJoinFinding, "Finding the inviter and syncing the room invite…");
    expect(en.panelRoomPanel, "Room panel");
    expect(en.roomDestPeople, "People");
    expect(en.roomDestAgents, "Agents & Runs");
    expect(en.roomDestFiles, "Files");
    expect(en.roomDestPipes, "Pipes");
    expect(en.roomDestActivity, "Activity");
    expect(en.roomNavLabel, "Room tools");
    expect(en.roomBackToRooms, "Back to Rooms");
    expect(en.roomBackToActivity, "Back to Activity");
    expect(en.roomCloseInspector, "Close inspector");
    expect(en.roomInformation, "Room information");
    expect(en.roomInfoRoom, "Room");
    expect(en.roomInfoSession, "Session");
    expect(en.roomInfoAgents, "Agents");
    expect(en.roomInfoInvites, "Invites");
    expect(en.memberStatusMember, "Member");
    expect(en.memberStatusUnknown, "Unknown");
    expect(en.pipeStateConnected, "Connected");
    expect(en.pipeStateOpen, "Open");
    expect(en.pipeStateClosed, "Closed");
    expect(en.fleetFilterLive, "Live");
    expect(en.roomLoadingMembers, "Loading members…");
    expect(en.panelMembersEmpty, "No members have synced for this room yet.");
    expect(en.panelMembersSummaryLabel, "Room members summary");
    expect(en.panelRosterCopy, "Roster from the signed room history. Statuses reflect membership events, not live peer reachability.");
    expect(en.panelMemberCountsLabel, "Member counts");
    expect(en.panelStatAgents, "Agents");
    expect(en.panelStatMembers, "Members");
    expect(en.panelStatInvited, "Invited");
    expect(en.panelRoomRoster, "Room roster");
    expect(en.panelThisDevice, "this device");
    expect(en.panelRoleOwner, "Owner");
    expect(en.panelRoleAgent, "Agent");
    expect(en.panelRoleMember, "Member");
    expect(en.panelLeave, "Leave");
    expect(en.panelOwnerStays, "Owner stays");
    expect(en.panelOwnerStaysTitle, "Owners cannot leave until ownership transfer exists.");
    expect(en.panelAgentsEmpty, "No agent members in this room yet. Invite one with role “agent”.");
    expect(en.panelNoStatusPostedYet, "No status posted yet");
    expect(en.panelFilesSummaryLabel, "Files summary");
    expect(en.panelNoSharedFilesYet, "No shared files yet");
    expect(en.panelFilesHeroEmptyDetail, "Share a readable path and peers can fetch a verified copy over P2P.");
    expect(en.panelFileAvailabilityLabel, "File availability");
    expect(en.panelFetchableNow, "Fetchable now");
    expect(en.panelProviderDevices, "Provider devices");
    expect(en.panelShareCardTitle, "Choose a file to share");
    expect(en.panelShareCardHelp, "Pick a local file. Jeliya uploads it to this daemon, imports it into the room blob store, and verifies it by content hash.");
    expect(en.panelHashCheckedBadge, "hash checked");
    expect(en.panelHashCheckedBadgeLabel, "Verified by content hash");
    expect(en.panelChooseFile, "Choose file");
    expect(en.panelChooseFileToShare, "Choose file to share");
    expect(en.panelNoFileSelectedYet, "No file selected yet.");
    expect(en.panelClearSelectedFile, "Clear");
    expect(en.panelShare, "Share");
    expect(en.panelSharing, "Sharing…");
    expect(en.panelAdvancedPathSummary, "Advanced: paste a daemon-readable path");
    expect(en.panelPathFieldLabel, "File path to share");
    expect(en.panelPathHint, "Use this only for files already under the daemon data directory.");
    expect(en.panelSharedInThisRoom, "Shared in this room");
    expect(en.panelAllFetchable, "All fetchable");
    expect(en.panelHealthServingToPeers, "Serving to peers");
    expect(en.panelHealthFetchedLocally, "Fetched locally");
    expect(en.panelHealthSecurityCheckFailed, "Security check failed");
    expect(en.panelHealthFetchFailed, "Fetch failed");
    expect(en.panelHealthReadyToFetch, "Ready to fetch");
    expect(en.panelKindBinary, "binary");
    expect(en.panelKindText, "text");
    expect(en.panelKindFile, "file");
    expect(en.panelPipesEmpty, "No pipes yet — expose a local port to one authorized peer below.");
    expect(en.panelConnect, "Connect");
    expect(en.panelConnecting, "Connecting…");
    expect(en.panelOpenPreview, "Open preview ↗");
    expect(en.panelClosePipe, "Close");
    expect(en.panelClosingPipe, "Closing…");
    expect(en.panelExposeTitle, "Expose a pipe");
    expect(en.panelExposeCopy, "Forward a local port to exactly one authorized peer.");
    expect(en.panelTargetFieldLabel, "Local target (host:port)");
    expect(en.panelAuthorizedPeerLabel, "Authorized peer");
    expect(en.panelNoOtherMembers, "no other members");
    expect(en.panelExpose, "Expose");
    expect(en.panelExposing, "Exposing…");
    expect(en.panelExposeDeniedTitle, "Can't expose this target");
    expect(en.panelExposeDeniedMessage, "Pipes may only forward a loopback port on this device.");
    expect(en.panelExposeDeniedAction, "Use a 127.0.0.1:<port> target, then retry.");
    expect(en.panelExposeInvalidTitle, "This target isn't valid");
    expect(en.panelExposeInvalidMessage, "The target must be a numeric ip:port on this device, and the peer a 64-hex identity ID.");
    expect(en.panelExposeInvalidAction, "Use a target like 127.0.0.1:3000, then retry.");
    expect(en.panelConnectInvalidTitle, "This pipe can't be connected");
    expect(en.panelConnectInvalidMessage, "The daemon does not know this pipe yet, or it is your own pipe — owners open their target directly instead of connecting.");
    expect(en.panelConnectInvalidAction, "Wait for the room to sync, or open the local target itself.");
    expect(en.inviteTitle, "Invite to room");
    expect(en.inviteIntro, "Tickets are bound to one identity. Ask the invitee for their identity ID — it is shown on their onboarding screen and in their sidebar footer, with a copy button.");
    expect(en.inviteRoomOpenForInviting, "This room is open for inviting.");
    expect(en.inviteRoomOpenForInvitingCopy, "Keep it open until the invitee finishes joining. Jeliya can only bootstrap them while an owner is reachable.");
    expect(en.inviteInviteeIdentityId, "Invitee identity ID");
    expect(en.inviteInviteePlaceholder, "64-hex identity ID");
    expect(en.inviteRoleLabel, "Role");
    expect(en.inviteExpiryLabel, "Expiry seconds");
    expect(en.inviteExpiryOptional, "(optional)");
    expect(en.inviteGenerateTicket, "Generate ticket");
    expect(en.inviteGenerating, "Generating…");
    expect(en.inviteExpiryErrorTitle, "This expiry isn't valid");
    expect(en.inviteExpiryErrorMessage, "Expiry must be a positive number of seconds.");
    expect(en.inviteExpiryErrorHint, "Leave it blank or use a value like 3600.");
    expect(en.inviteReadyToSend, "Ready to send.");
    expect(en.inviteReadyToSendCopy, "Stay in this room until they join. If they still see “Couldn't reach the inviter,” copy a fresh invite and retry.");
    expect(en.inviteCombinedCopy, "Send this one paste to the invitee — it is the ticket and your dialable address together. They paste it into “Join with a ticket” and the address fills in automatically.");
    expect(en.inviteCombinedInviteLabel, "Combined invite (ticket and peer address)");
    expect(en.inviteCopyInvite, "Copy invite");
    expect(en.inviteShareInvite, "Share invite");
    expect(en.inviteSeparatelySummary, "Send the ticket and address separately");
    expect(en.inviteInviteTicketLabel, "Invite ticket");
    expect(en.inviteCopyTicket, "Copy ticket");
    expect(en.inviteShareTicket, "Share ticket");
    expect(en.inviteCopyAddress, "Copy address");
    expect(en.inviteNewInvite, "← New invite");
    expect(en.inviteNoDialableAddress, "No dialable address reported yet.");
    expect(en.inviteNoDialableAddressCopy, "Keep this room open. The joiner may still connect via discovery or relay, but a fresh room address is more reliable.");
    expect(en.inviteTicketOnlyCopy, "Send this ticket to the invitee. They join with it (room.join).");
    expect(en.inviteNoDialableAddressNote, "This daemon has not reported a dialable address — the joiner may connect via relay or discovery.");
    expect(en.renamePeerTitle, "Name this peer");
    expect(en.renamePeerCopy, "Local alias only — names never leave this machine.");
    expect(en.renamePeerIdentityLabel, "Identity:");
    expect(en.renamePeerAliasLabel, "Alias");
    expect(en.renamePeerAliasPlaceholder, "e.g. Maya R.");
    expect(en.renamePeerSave, "Save");
    expect(en.renamePeerClearAlias, "Clear alias");
    expect(en.fetchOpenLocalFileCopy, "Open local file copy");
    expect(en.roomHeaderNoPeersConnected, "No peers connected");
    expect(en.roomHeaderPeerToPeer, "Peer-to-Peer");
    expect(en.roomHeaderRelayOnly, "Relay only");
    expect(en.roomHeaderShareFile, "Share file");
    expect(en.roomHeaderOpenPipe, "Open pipe");
    expect(en.roomHeaderInvite, "Invite");
    expect(en.roomHeaderPeerConnections, "Peer connections");
    expect(en.roomHeaderPeerStateConnected, "connected");
    expect(en.roomHeaderPeerStateConnecting, "connecting");
    expect(en.roomHeaderPeerStateOffline, "offline");
    expect(en.settingsTitle, "Settings");
    expect(en.settingsLanguageLabel, "Language");
    expect(en.settingsFormattingLabel, "Dates & numbers");
    expect(en.settingsLocaleSystemDefault, "System default");
    expect(en.settingsIdentityLabel, "P2P Identity");
    expect(en.settingsDeviceLabel, "Device");
    expect(en.settingsCopyDeviceId, "Copy device ID");
    expect(en.settingsIdentityNote, "Unrecoverable if this device or its data folder is lost.");
    expect(en.settingsEndpointLabel, "Endpoint");
    expect(en.settingsRelayLabel, "Relay");
    expect(en.settingsCopyEndpointId, "Copy endpoint ID");
    expect(en.settingsDaemonLabel, "Daemon");
    expect(en.settingsVersionLabel, "Version");
    expect(en.settingsProtocolLabel, "Protocol");
    expect(en.settingsPidLabel, "PID");
    expect(en.settingsPortLabel, "Port");
    expect(en.settingsDataDirLabel, "Data folder");
    expect(en.settingsTransportLabel, "Transport");
    expect(en.settingsSupervisorLabel, "Supervisor");
    expect(en.settingsSupervisorOwned, "Launched by this app");
    expect(en.settingsSupervisorAdopted, "Adopted a running daemon");
    expect(en.settingsSupportLabel, "Support");
    expect(en.settingsDiagnosticsTitle, "Diagnostics");
    expect(en.settingsDiagnosticsCopy, "Copy a privacy-safe snapshot for bug reports: daemon version, connection state, room counts, peer state, file-transfer state, pipe state, and the latest UI error.");
    expect(en.settingsNoMessageBodies, "No message bodies");
    expect(en.settingsNoInviteTickets, "No invite tickets");
    expect(en.settingsNoFileNamesOrPaths, "No file names or full local paths");
    expect(en.settingsNoFullIdentityIds, "No full identity IDs");
    expect(en.settingsLastCapturedError, "Last captured error");
    expect(en.settingsNoErrorCaptured, "No UI action error captured in this session.");
    expect(en.settingsCopyDiagnostics, "Copy diagnostics");
    expect(en.settingsCopiedDiagnostics, "Copied diagnostics");
    expect(en.settingsReportIssue, "Report issue");
    expect(en.shellSelectRoom, "Select a room");
    expect(en.shellUntitledRoom, "Untitled room");
    expect(en.shellBannerDisconnected, "Disconnected from daemon.");
    expect(en.shellConnConnected, "Connected");
    expect(en.shellConnConnecting, "Connecting…");
    expect(en.shellConnReconnecting, "Reconnecting…");
    expect(en.shellConnDisconnected, "Disconnected");
    expect(en.sidebarProfileTitle, "Profile & settings");
    expect(en.sidebarNavPrimaryLabel, "Primary");
    expect(en.sidebarNavRooms, "Rooms");
    expect(en.sidebarNavFleet, "Agent Fleet");
    expect(en.sidebarNavPipes, "Pipes");
    expect(en.sidebarNavFiles, "Files");
    expect(en.sidebarNavSettings, "Settings");
    expect(en.sidebarYourRooms, "Your Rooms");
    expect(en.sidebarCreateRoomIcon, "Create room");
    expect(en.sidebarRoomsListLabel, "Rooms");
    expect(en.sidebarNoRoomsYet, "No rooms yet");
    expect(en.sidebarStateOpen, "Open");
    expect(en.sidebarStateClosed, "Closed");
    expect(en.sidebarStateLeft, "Left");
    expect(en.sidebarStateRemoved, "Removed");
    expect(en.sidebarSessionOpen, "Session open");
    expect(en.sidebarLeftRoomTitle, "You left this room");
    expect(en.sidebarRemovedRoomTitle, "You were removed from this room");
    expect(en.sidebarSearchRooms, "Search rooms…");
    expect(en.sidebarSearchLabel, "Search rooms by name or short id");
    expect(en.sidebarFilterLabel, "Filter rooms by lifecycle");
    expect(en.sidebarFilterAll, "All");
    expect(en.sidebarFilterActive, "Active");
    expect(en.sidebarLifecycleDeparted, "Left & removed");
    expect(en.sidebarSectionPinned, "Pinned");
    expect(en.sidebarSectionArchived, "Archived");
    expect(en.sidebarUnread, "Unread");
    expect(en.sidebarNoRoomsInFilter, "No rooms in this filter.");
    expect(en.sidebarClearSearch, "Clear");
    expect(en.sidebarP2pIdentity, "P2P Identity");
    expect(en.timelineRoomTimeline, "Room timeline");
    expect(en.timelineEmptyState, "No events yet — say something below.");
    expect(en.timelineAgentChip, "AGENT");
    expect(en.timelineStatusFallback, "status");
    expect(en.timelineSharedAFile, "shared a file");
    expect(en.timelineOpenedAPipe, "opened a pipe");
    expect(en.timelineOpenInPipes, "Open in Pipes");
    expect(en.timelineSomeone, "someone");
    expect(en.timelinePendingSending, "Sending…");
    expect(en.timelinePendingSyncing, "Sent locally, syncing…");
    expect(en.timelinePendingFailed, "Couldn't send");
    expect(en.timelineToday, "Today");
    expect(en.timelineYesterday, "Yesterday");
    expect(en.timelineBytesUnknown, "?");
    expect(en.commonCopy, "Copy");
    expect(en.commonCopied, "Copied ✓");
    expect(en.commonRetry, "Retry");
    expect(en.commonCopyIdentityId, "Copy identity ID");
    expect(en.commonServing, "Serving");
    expect(en.commonServingTooltip, "This daemon is already serving this file to peers.");
    expect(en.commonFileExtFallback, "FILE");
    expect(en.commonClose, "Close");
    expect(en.commonTechnicalDetails, "Technical details");
    expect(en.commonYou, "You");
    expect(en.commonClickToSetLocalName, "Click to set a local name");
    expect(en.commonTaskProgress, "Task progress");
    expect(en.commonFetch, "Fetch");
    expect(en.commonFetching, "Fetching…");
    expect(en.commonChecking, "Checking…");
    expect(en.commonVerified, "✓ Verified");
    expect(en.commonFetched, "✓ Fetched");
    expect(en.commonFailed, "✕ Failed");
    expect(en.commonRecheck, "Recheck");
    expect(en.commonOpenFile, "Open file");
    expect(en.commonCopyPath, "Copy path");
    expect(en.commonCopySavedFilePath, "Copy saved file path");
    expect(en.commonNoProviderOnline, "No provider online");
    expect(en.wireRoleOwnerInline, "owner");
    expect(en.wireRoleMemberInline, "member");
    expect(en.wireRoleAgentInline, "agent");
    expect(en.wireStatusInvited, "Invited");
    expect(en.wireStatusLeft, "Left");
    expect(en.wireStatusRemoved, "Removed");
    expect(en.wirePathDirect, "direct");
    expect(en.wirePathRelay, "relay");
    expect(en.wireModeLoopback, "loopback");
    expect(en.wireModeReal, "real");
    expect(en.wireConnConnectedInline, "connected");
    expect(en.wireConnConnectingInline, "connecting");
    expect(en.wireConnReconnectingInline, "reconnecting");
    expect(en.wireConnDisconnectedInline, "disconnected");
    expect(en.panelPathPlaceholder, "/path/to/report.pdf");
  });

  test('every parameterized en entry renders exactly (both plural branches)', () {
    expect(en.bootAdoptedDaemon(7, 13), "adopted a running daemon (pid 7) on :13");
    expect(en.bootDaemonUp(7, 13), "daemon up (pid 7) on :13, connecting…");
    expect(en.bootProtocolMismatch(7, 13), "The daemon speaks protocol v7 but this app requires v13 — update the app or the daemon, then retry.");
    expect(en.composerMessagePlaceholder('XroomNameX'), "Message XroomNameX");
    expect(en.fleetStatOfTotal(7), "of 7 total");
    expect(en.fleetStatRoomsCovered(7, 13), "7 of 13 rooms");
    expect(en.fleetLastUpdate('XrelX'), "Last update XrelX");
    expect(en.fleetLastStatus('XlabelX'), "Last: XlabelX");
    expect(en.fleetRefreshedAt('XrelX'), "Updated XrelX");
    expect(en.fleetSparkEvents(7), "7 status events");
    expect(en.fleetSparkEvents(1), "1 status event");
    expect(en.fleetSparkEvents(2), "2 status events");
    expect(en.fleetRelMinutesAgo(7), "7m ago");
    expect(en.fleetRelHoursAgo(7), "7h ago");
    expect(en.fleetRelDaysAgo(7), "7d ago");
    expect(en.addAgentIntro('XemphasisX'), "Mint an agent-role ticket for a room you own. This XemphasisX — running the command below on the agent’s machine is a deliberate, human step (the security boundary).");
    expect(en.addAgentGuidance('XnpmX', 'XjeliyadX', 'XprefixX', 'XguideX'), "The runner lives in the repo — clone it and run this from the checkout (no XnpmX needed; Node 22+ required). Installed XjeliyadX via brew/script instead of building? Prefix the command with XprefixX so the runner finds it. Full guide: XguideX.");
    expect(en.roomShortIdLabel('XshortIdX'), "Room ID XshortIdX");
    expect(en.modalJoinCopy('XcombinedX'), "Paste the invite you received. A combined invite (XcombinedX) fills in the peer address automatically.");
    expect(en.modalLeaveCopy('XroomX'), "Leaving XroomX publishes a signed membership departure. This is different from closing the local session; you’ll need a new invite to join again.");
    expect(en.onboardingJoinAttempt(7, 13), "Attempt 7/13");
    expect(en.onboardingJoinRetryingAttempt(7, 13), "Retrying join (7/13)…");
    expect(en.onboardingJoinRetryWait(7), "The first path did not answer. Retrying in 7s…");
    expect(en.panelRoomMemberCount(7), "7 room members");
    expect(en.panelRoomMemberCount(1), "1 room member");
    expect(en.panelRoomMemberCount(2), "2 room members");
    expect(en.panelAgentStatusFooter('XstatusX'), "status: XstatusX");
    expect(en.panelSharedFileCount(7), "7 shared files");
    expect(en.panelSharedFileCount(1), "1 shared file");
    expect(en.panelSharedFileCount(2), "2 shared files");
    expect(en.panelFilesHeroDetail('XtotalBytesX', 7), "XtotalBytesX in the room · 7 fetchable here");
    expect(en.panelNFetched(7), "7 fetched");
    expect(en.panelFetchableNowValue(7, 13), "7/13");
    expect(en.panelServedByYou(7), "7 served by you");
    expect(en.panelAwaitingProvider(7), "7 awaiting a provider");
    expect(en.panelNProviders(7), "7 providers");
    expect(en.panelNProviders(1), "1 provider");
    expect(en.panelNProviders(2), "2 providers");
    expect(en.panelPipeMeta('XopenedByX', 'XauthorizedX'), "by XopenedByX · authorized: XauthorizedX");
    expect(en.panelPeerChoice('XnameX', 'XroleX'), "XnameX (XroleX)");
    expect(en.fetchProvidersListedOnline(7), "7 providers listed; at least one is online");
    expect(en.fetchProvidersListedOnline(1), "1 provider listed; at least one is online");
    expect(en.fetchProvidersListedOnline(2), "2 providers listed; at least one is online");
    expect(en.fetchProvidersListedOffline(7), "7 providers listed; none are online right now");
    expect(en.fetchProvidersListedOffline(1), "1 provider listed; none are online right now");
    expect(en.fetchProvidersListedOffline(2), "2 providers listed; none are online right now");
    expect(en.fetchVerifiedTooltip('XpathX'), "verified · XpathX");
    expect(en.fetchFetchedTooltip('XpathX'), "fetched · XpathX");
    expect(en.fetchDetailVerified('XbytesX', 'XpathX'), "Verified · XbytesX · saved to XpathX");
    expect(en.fetchDetailFetched('XbytesX', 'XpathX'), "Fetched · XbytesX · saved to XpathX");
    expect(en.roomHeaderAgentCount(7), "7 agents");
    expect(en.roomHeaderAgentCount(1), "1 agent");
    expect(en.roomHeaderAgentCount(2), "2 agents");
    expect(en.roomHeaderInvitesPending(7), "7 invites pending");
    expect(en.roomHeaderInvitesPending(1), "1 invite pending");
    expect(en.roomHeaderInvitesPending(2), "2 invites pending");
    expect(en.settingsDaemonSummary('XmodeX', 'XconnX'), "XmodeX · XconnX");
    expect(en.shellBannerReconnecting('XwsUrlX'), "Connection to daemon lost — reconnecting… (XwsUrlX)");
    expect(en.sidebarProfileHandle('XdeEllipsizedShortIdX'), "@XdeEllipsizedShortIdX");
    expect(en.sidebarRoomMeta(7, 'XstateLabelX'), "7 members · XstateLabelX");
    expect(en.sidebarRoomMeta(1, 'XstateLabelX'), "1 member · XstateLabelX");
    expect(en.sidebarRoomMeta(2, 'XstateLabelX'), "2 members · XstateLabelX");
    expect(en.sidebarPinRoom('XroomNameX'), "Pin XroomNameX");
    expect(en.sidebarUnpinRoom('XroomNameX'), "Unpin XroomNameX");
    expect(en.sidebarArchiveRoom('XroomNameX'), "Archive XroomNameX");
    expect(en.sidebarRestoreRoom('XroomNameX'), "Restore XroomNameX");
    expect(en.sidebarNoRoomsMatch('XqueryX'), "No rooms match “XqueryX”.");
    expect(en.sidebarEndpointShort('XshortEndpointIdX'), "ep XshortEndpointIdX");
    expect(en.sidebarEndpointTitle('XendpointIdX'), "endpoint XendpointIdX");
    expect(en.timelineFileMeta('XbytesX', 'XextX'), "XbytesX · XextX");
    expect(en.timelineAuthorizedPeer('XpeerX'), "authorized peer: XpeerX");
    expect(en.timelineSyslineRoomCreated('XsenderX', 'XtimeX'), "XsenderX created the room · XtimeX");
    expect(en.timelineSyslineInvited('XsenderX', 'XinviteeX', 'XroleX', 'XtimeX'), "XsenderX invited XinviteeX as XroleX · XtimeX");
    expect(en.timelineSyslineJoined('XwhoX', 'XroleX', 'XtimeX'), "XwhoX joined as XroleX · XtimeX");
    expect(en.timelineSyslineLeft('XwhoX', 'XtimeX'), "XwhoX left the room · XtimeX");
    expect(en.timelineSyslinePipeClosed('XsenderX', 'XtargetX', 'XtimeX'), "XsenderX closed pipe XtargetX · XtimeX");
    expect(en.timelineNewMessages(7), "7 new messages");
    expect(en.timelineNewMessages(1), "1 new message");
    expect(en.timelineNewMessages(2), "2 new messages");
    expect(en.timelineBytesB(7), "7 B");
    expect(en.timelineBytesKb(7), "7 KB");
    expect(en.timelineBytesMb('XnX'), "XnX MB");
    expect(en.timelineBytesGb('XnX'), "XnX GB");
    expect(en.commonOptionalFieldLabel('XlabelX', 'XoptionalX'), "XlabelX XoptionalX");
    expect(en.commonMemberCount(7), "7 members");
    expect(en.commonMemberCount(1), "1 member");
    expect(en.commonMemberCount(2), "2 members");
    expect(en.commonPercent('XnX'), "XnX%");
  });
}
