target/
# The one canonical Dioxus web output (§9, O-5). Built by `scripts/build-web.sh`
# from a clean checkout; never committed, so no stale artifact can be embedded.
dist/
dist-a/
dist-b/
test-results/
playwright-report/
# Symlinked to a repo Playwright install for the no-network render smoke, not
# committed.
node_modules
